"""model_eval — HyphAeon neural model behavior evaluation.

This package is intentionally not included in the installed ``hyphaeon``
wheel/bioconda package (``pyproject.toml``: ``packages = ["hyphaeon"]``).
It only works from a source clone and provides the ``python -m model_eval``
concordance CLI plus the pytest evaluation suites.
"""
